package lapsang.builtin
    // Provides a set of values that correspond to get, set, constructor,
    // along with private, internal and public.
    public enum accessorType:
        publicGet,
        internalGet,
        privateGet,
        publicSet,
        internalSet,
        privateSet,
        publicConstructor,
        internalConstructor,
        privateConstructor

package lapsang.builtin
    //
    // True if instance1 and instance2 reference the same item (ie, point to the same address)
    //
    public builtin bool sameInstance(mutable instance1, mutable instance2)
    
    //
    // Returns a string representation of a primitive value 
    //
    public builtin string primitiveToString(primitive value)